import express from 'express'
import path, {dirname} from 'path'
import { fileURLToPath } from 'url'


const app = express()
const PORT = 3000;


const __dirname = __filename(path.join{meta.url})
const __filename = __filename(__dirname)


app.get('/me', (req, res)=>{
    app.sendFile( express.static(path.join('public', me.html)))


})

app.listen('/me', (req, res)=>{
    console.log(express.static(path.join('public', 'me.html')))
})

const.db = createConnection({
    host:'',
user:'root',

p
datatabse 'gcg'
})







